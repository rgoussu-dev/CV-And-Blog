{
  "name": "cv-and-blog-blog-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static tech-blog generator. Reads ../blog/posts/*.md and renders the blog into dist/ (served under /blog/). Sibling of Alfred's cv-site.",
  "scripts": {
    "build": "tsx src/build.ts",
    "serve": "tsx src/serve.ts",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
